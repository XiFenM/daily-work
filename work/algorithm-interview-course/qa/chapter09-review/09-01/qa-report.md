# 09-01 人工 QA 报告

## 范围

- 课次：`09-01 什么是动态规划`
- 原片：`work/algorithm-interview-course/incoming/9-1 什么是动态规划_慕课网.mp4`
- 实测时长：`1230.960 s`
- 规范化证据：`outputs/algorithm-interview-course/understanding/normalized/09-01.json`
- 正式笔记：`projects/algorithm-interview-course/notes/lessons/09-01.md`
- 检查日期：2026-08-03

## 视觉与时间线

- 检查了初始联系表、`contact-50s.jpg` 以及 05:20–18:20 的定向帧。
- 人工纠正模型误分段：递归树是 05:40–07:37，调用次数是 07:37–08:38，记忆化实现是 08:38–12:08，自底向上是 14:54–17:27。
- 课件约 01:03 显示 `F(0)=1,F(1)=1`；三版代码均为 `fib(0)=0,fib(1)=1`。规范化证据与笔记并列记录冲突。
- 屏幕数字复核：纯递归 `n=20` 调用 21891 次，`n=40` 调用 331160281 次；记忆化 `n=40` 调用 79 次，`n=1000` 调用 1999 次。
- `n=1000` 的 `int` 结果为 `1556111435`，课程明确说明已经溢出；记忆化画面耗时为 `6.5e-05 s`，自底向上画面耗时为 `6e-05 s`。

## 代码复核

编译选项：`clang++ -std=c++11 -Wall -Wextra -pedantic`。

| 代码       | 代表测试                | 结果     | 规范化状态 |
| ---------- | ----------------------- | -------- | ---------- |
| `code-001` | `n=0,1,2,10,20`         | 全部通过 | `tested`   |
| `code-002` | `n=0,1,2,10,40`         | 全部通过 | `tested`   |
| `code-003` | `n=1,2,10,40`           | 全部通过 | `failed`   |
| `code-003` | `n=0`，AddressSanitizer | 越界失败 | `failed`   |

`code-003` 在 `n=0` 时建立长度 1 的数组后写入 `memo[1]`，AddressSanitizer 确认为 `heap-buffer-overflow`。状态按完整问题边界记为 `failed`，没有用正输入通过掩盖缺陷。

## 证据与复杂度结论

- `course_direct`：纯递归时间 `O(2^n)`；记忆化和自底向上时间 `O(n)`。
- 课程没有直接给出空间复杂度；规范化顶层空间保持 `null`。
- 滚动变量 `O(1)` 空间未在课程展示，仅作为笔记中的整理扩展。

## 修改边界

- raw 响应、原片、压缩片均未修改。
- 只修订 normalized 中的时间线、状态模型、正确性义务、验证状态与证据边界。
- QA 图、测试外壳和本报告位于 Git 忽略的 `work/`。

## 未验证项

- 未进行在线评测提交。
- 未复现实验机器、编译优化和性能数字。
- 未验证其他编译器及大整数实现。

## 自动校验

- canonical JSON Schema：通过。
- normalized 证据 ID 引用与时间上限检查：通过。
- 正式笔记 Prettier：通过。
- `git diff --check`：通过。
- 项目级 `validate-project.mjs` 在第九章全部登记后通过：66 份 normalized、66 份单课笔记、9 份章节综述、77 次尝试、74 次 generation 与 88 条 verified 关系均完成对账。
